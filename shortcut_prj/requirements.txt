# =========================================
# ⚡ 쇼특허 (Short-Cut) Django v1.0
# =========================================

# 1. Django Core
django>=5.0.0
djangorestframework
django-environ         # .env 파일 관리용

# 2. Data & AI (기존 src 로직용)
numpy>=1.26.0
pandas>=2.0.0
scipy>=1.12.0
openai>=1.12.0
pinecone-client        # 벡터 DB 연결
pinecone-text          # 하이브리드 검색용
tiktoken               # 토큰 카운팅
pydantic>=2.6.0

# 3. Utilities
python-dotenv          # 환경변수 로드
tqdm                   # 진행바 (터미널용)
aiofiles               # 비동기 파일 처리
orjson                 # 빠른 JSON 처리 (선택)

# 4. NLP (기존 프로젝트 사양)
# 설치 후: python -m spacy download en_core_web_sm 실행 필요
spacy>=3.7.0

# 5. Async Support (Django-Async 호환)
asyncio

# 용어 사전 다운로드에 필요한 패키지
xhtml2pdf